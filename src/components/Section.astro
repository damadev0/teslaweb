---
// Still have to receive Footer and small bottom letter
const { dataHeaderColor, image, title, subtitle, description, buttons, bottomText, footerLinks } = Astro.props
const darkButton = "bg-zinc-900/[.6] text-white"
const lightButton = "bg-white text-zinc-700"

---

<section
  class="landing-section bg-black w-full h-screen text-center relative overflow-hidden"
  data-header-color={dataHeaderColor}
>
  <div class="z-30 relative h-full flex flex-col">

    <!-- Titles -->
    <header class="pt-20">
      <h2 class={`text-${dataHeaderColor} text-[40px] font-medium`}>{title}</h2>
      { subtitle && <p class={`text-${dataHeaderColor} text-sm ${subtitle.underline && "underline"}`} >{subtitle.text}</p>}
      { description && <p class={`text-${dataHeaderColor} text-sm`}>{description.text}</p> }
    </header>

    <!-- Buttons -->
    <footer class="flex flex-col flex-grow justify-end pb-8">
      <div class="flex gap-x-6 mx-auto">
        {buttons.map(button => (
          <a
            class={`${button.lightTheme ? lightButton : darkButton} opacity-75 hover:scale-105 duration-300 backdrop-blur rounded font-medium  px-20 py-3 inline-block [&>span]:text-sm`}
            href={button.href}
          >
            <span>{button.text}</span>
          </a>
        ))
        }
      </div>
    </footer>

    <!-- Legal text -->
    {bottomText && <div class={`pb-4 text-xs text-${dataHeaderColor}`}>  <p>{bottomText}</p> </div> }
  
    <!-- Footer Links -->
    { footerLinks &&
      <nav class="mb-20">
        <ul class="flex justify-center gap-x-6 mx-auto [&>li>a]:text-xs [&>li>a]:font-medium [&>li>a]:text-white">
          {footerLinks.map(link => (
            <li><a href={link.href}>{link.text}</a></li>
          ))
          }
        </ul>
      </nav>
    }
  </div>

  <!-- Background Image !! Modify alt !! -->
  <div class="absolute top-0 bottom-0 h-full w-full z-10">
    <img
      src={image}
      alt="Model S"
      class="h-full w-full object-center object-cover"
    />
  </div>
</section>

